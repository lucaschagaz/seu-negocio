{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "auth": "node server.js --no-cors",
    "backend": "json-server --watch db.json --port 4000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "json-server": "^0.17.0",
    "jsonwebtoken": "^8.5.1"
  }
}
